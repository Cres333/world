{
  "name": "world",
  "version": "0.1.0",
  "homepage": "https://cres333.github.io/world",
  "private": true,
  "dependencies": {
    "@material-ui/core": "*",
    "@material-ui/icons": "*",
    "@testing-library/jest-dom": "*",
    "@testing-library/react": "*",
    "@testing-library/user-event": "*",
    "react": "^18",
    "react-color": "*",
    "react-copy-to-clipboard": "*",
    "react-dom": "^18",
    "react-router-dom": "^5",
    "react-scripts": "^5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn run build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
